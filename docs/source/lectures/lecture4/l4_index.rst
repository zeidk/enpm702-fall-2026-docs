.. _l4_index:

====================
L4: STL Containers
====================

Overview
--------

This lecture covers the Standard Template Library (STL) containers -- strings, arrays, and vectors. Students will learn about container memory management, access and modification patterns, and iterators. By understanding how the STL organizes and manages data, you will be equipped to choose the right container for a given task and use it effectively.

Learning Objectives
-------------------

By the end of this lecture, you will be able to:

1. Describe the STL and its role in the C++ Standard Library, including the four categories of containers.
2. Distinguish between C-style strings (C-strings) and ``std::string``, and explain why ``std::string`` is preferred.
3. Perform common string operations including initialization, concatenation, insertion, removal, and searching.
4. Compare ``std::array`` with C-style arrays and explain the advantages of ``std::array``.
5. Declare and use multidimensional arrays with both C-style and ``std::array`` syntax.
6. Use ``std::vector`` for dynamic arrays, understanding its automatic memory management.
7. Use iterators to traverse containers and understand the iterator abstraction.
8. Explain container memory management concepts including capacity, size, ``reserve()``, and ``shrink_to_fit()``.

.. toctree::
   :maxdepth: 2
   :hidden:
   :caption: Lecture 4 Contents

   l4_lecture
   l4_shell
   l4_exercises
   l4_quiz
   l4_references

Next Steps
----------

In **Lecture 5: Functions Basics**, we will explore how to define and call functions in C++, including function parameters, return types, overloading, and default arguments. Functions are the fundamental building blocks for organizing code into reusable, modular units.
